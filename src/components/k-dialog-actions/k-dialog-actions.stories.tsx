import React from 'react';
import { KDialogActions } from '../../../component-library-react/src/components';

export default {
  title: 'Components/Dialog',
  component: 'k-dialog-actions'
};

const Template = () => (
  <KDialogActions>
    Some action
  </KDialogActions>
);

export const DialogActions = Template.bind({});